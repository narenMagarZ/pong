export const paddle = {
  x: 0, // default x position
	y: 200,
	width: 10,
	height: 120,
	targetY: 200, // Destination position
	speed: 30, // Fixed movement per key press
	lerpFactor: 0.2, // Smoothness factor
}