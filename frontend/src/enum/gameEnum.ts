export enum GameStatusEnum {
  notStarted = 'notStarted',
  created = 'created',
  inProgress = 'inProgress',
  paused = 'paused',
  end = 'end'
}

export enum ModalTypeEnum {
  "gameInit" = "gameInit",
  "gamePause" = "gamePause",
  "gameEnd" = "gameEnd"
}